# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.show(page = "index")

# Blog pages
GET     /blog                       controllers.Blog.list()
GET     /blog/admin                 controllers.Blog.admin()
GET     /blog/post/create           controllers.Blog.createPost()

# Security stuff
GET     /login                      controllers.Login.form()
GET     /blog/logout                controllers.Login.logout()
POST    /blog/authenticate          controllers.Login.authenticate()

# Default pages
GET     /:page                      controllers.Application.show(page)

# Map static resources from the /public folder to the /assets URL path
GET     /static/*file               controllers.Assets.at(path="/public", file)
