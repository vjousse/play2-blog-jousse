# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.show(page = "index")

# Blog pages
GET     /blog                       controllers.Blog.list()
GET     /blog/login                 controllers.Blog.login()
GET     /blog/logout                controllers.Blog.logout()
GET     /blog/admin                 controllers.Blog.admin()
POST    /blog/authenticate          controllers.Blog.authenticate()

# Default pages
GET     /:page                      controllers.Application.show(page)

# Map static resources from the /public folder to the /assets URL path
GET     /static/*file               controllers.Assets.at(path="/public", file)
